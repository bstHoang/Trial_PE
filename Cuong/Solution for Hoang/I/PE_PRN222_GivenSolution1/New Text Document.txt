(0.1) Liên tục nhắc người dùng nhập số nguyên (ID Genre) qua console với Message “Please enter integer number :”
pass
(0.1) Nếu người dùng nhấn Enter (nhập rỗng), chương trình sẽ kết thúc , hiển thị message “Exiting client application…”.
pass
(0.1) Kiểm tra xem đầu vào của người dùng có phải là số hợp lệ không. Nếu đầu vào không phải số nguyên hợp lệ, hiển thị thông báo lỗi "Invalid input! Please enter a valid integer" và nhắc người dùng thử lại.
pass
(0.1)Gửi số nguyên (ID Genre) mà người dùng đã nhập đến server. Nếu server không chạy hoặc kết nối thất bại, hiển thị thông báo lỗi "Server is not running. Please try again later." và quay lại chờ nhập liệu mới từ người dùng.
pass
(0.2) Nhận chuỗi JSON từ server sau khi client gửi request đến server và deserialize nó thành danh sách sách . Mỗi sách bao gồm: ID, Title, Publication Year, Genre Name. Hiển thị thông tin từng sách trên console theo định dạng đã cho trong lớp Util. 
not pass, format error
(0.2)Nếu danh sách sách rỗng, hiển thị thông báo "No books found by genre ID {genreId}". Sau khi xử lý phản hồi, quay lại chờ nhập liệu mới từ người dùng, trừ khi người dùng đã nhập chuỗi rỗng.
pass
